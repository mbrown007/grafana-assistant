we will not be using docker we should write the app in go so can be easily packaged into binary.

we should have security front and center

the iframe wrapper and chat app will be on same machine as grafana / prometheus / alertmanager

We should write tests as we go.

Security should be fornt and center this will be customer facing app.

We should have user login, with chat history attached to the user.

